import 'package:flutter/material.dart';

class UserTableScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('User Table Screen'),
    );
  }
}
