// admin_dashboard.dart
import 'package:flutter/material.dart';

class AdminTable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Admin Table'),
    );
  }
}
